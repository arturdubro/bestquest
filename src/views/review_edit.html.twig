{% extends "layout.html.twig" %}
{% block content %}
<div class="container">
<a href="/admin" class="back_to_main">Назад</a>
{{ form_start(form) }}
	<div class="one_position">
		{{ form_row(form.project) }}
	</div>
	<div class="one_position">
		{{ form_row(form.name) }}
	</div>
	<div class="one_position">
		{{ form_row(form.company) }}
	</div>
	<div class="one_position">
		{{ form_row(form.position) }}
	</div>
	<div class="one_position">
		{{ form_row(form.review) }}
	</div>
	<div class="one_position">
		{{ form_label(form.avatar) }}
		<img src="/project_files/{{selected_review.project_id}}/reviews/{{selected_review.author_avatar}}" alt="Фотографии нет"/>
		<p class="change_image">Заменить</p>
		
		{{ form_errors(form.avatar) }}
		{{ form_widget(form.avatar) }}
	</div>
<input type="submit" value="Редактировать"/>
{{ form_end(form) }}
</div>
<script>
	var change_img = document.getElementsByClassName('change_image');
	for (var i=0; i<change_img.length; i++){
		change_img[i].onclick = function(){
			this.nextElementSibling.style.display = 'block';
		}
	}
</script>
{% endblock %}